<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured= {{devour}}>
		{{#if devoured}}YUMM!{{else}}EAT IT AGAIN!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>